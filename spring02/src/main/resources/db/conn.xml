<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<context:property-placeholder location="classpath:db.properties"/>
	<!-- DBCP 객체 생성 - Spring 컨테이너가 관리 -->
	<bean id="dbcp" class="org.apache.commons.dbcp2.BasicDataSource">
		<property name="driverClassName" value="${oracle}"/>
		<property name="url" value="${url}"/>
		<property name="username" value="${username}"/>
		<property name="password" value="${password}"/>
	</bean>
	
	<!-- Mybatis 사용을 위한 SqlSessionFActory 객체 생성 : SqlSession + SqlSessionBilder -->
	<bean id='factory' class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref='dbcp'></property> <!-- id 값을 통해, DBCP 객체 주소 가져옴 -->
		<property name="mapperLocations" value='classpath:sqlmap/*-mapper.xml'></property>
	</bean>
	
	<!-- 쿼리문 실행 객체(SqlSession -> SqlSessionTemplate) 생성 -->
	<bean class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg name="sqlSessionFactory" ref="factory"></constructor-arg>
		<!-- value="factory" -> ref="factory" 주소 가져옴-->
	</bean>
</beans>
